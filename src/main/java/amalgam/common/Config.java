package amalgam.common;

public class Config {
    // TODO add config gui
    // TODO allow configuration of base amalgam amount (nugget amount) in mB (milliBuckets) it is currently set to 10;
    // TODO allow disabling of vanilla tool and armor recipes
}
